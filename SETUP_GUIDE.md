# BU3 출장 일정 관리 도구 — 설치/설정 가이드

이 문서 하나로 처음부터 끝까지 우리 조직만의 독립된 사본을 만들 수 있습니다. 코드/Git 경험이 없어도 순서대로 따라 하면 됩니다.

## 0. 개요

이 도구는 다음 3개 요소로 동작합니다.

```
브라우저 (GitHub Pages의 정적 웹페이지)
      ↕  (인터넷을 통해 데이터를 주고받음)
Google Apps Script 웹앱 (데이터를 읽고 쓰는 중간 서버 역할)
      ↕
Google Sheets (실제 데이터가 저장되는 곳, 엑셀과 비슷한 구글 스프레드시트)
```

- 화면(간트차트/인원 출장일/설비 진행율/이력관리 4개 탭)은 정적 파일이라 GitHub Pages에 올려두면 무료로 호스팅됩니다.
- 실제 데이터는 우리 조직의 Google Sheets에 저장되고, Apps Script가 그 사이를 중계합니다.

**필요한 것**: GitHub 계정 1개, Google 계정 1개(회사 구글 워크스페이스 계정도 가능). **원 제작자의 계정이 아니라 우리 조직 자체의 계정으로 새로 시작해야 합니다.**

**예상 소요 시간**: 처음 세팅 기준 30~40분.

**압축파일 안에 들어있는 것**:
- 웹페이지 코드 10개 파일 (`index.html`, `data.js`, `app.js`, `gantt.js`, `modals.js`, `person.js`, `equip.js`, `vision.js`, `export.js`, `style.css`)
- `GAS_backend_code.txt` — Google Apps Script에 붙여넣을 백엔드 코드
- 이 가이드 (`SETUP_GUIDE.md`)

모든 데이터(사이트/인원/일정)와 접속 정보는 비어있는 상태로 정리되어 있으니, 아래 순서대로 진행하면 됩니다.

---

## 1. GitHub 저장소 생성

1. [github.com](https://github.com)에서 계정이 없으면 가입합니다.
2. 로그인 후 우측 상단 `+` → `New repository` 클릭.
3. 저장소 이름을 정합니다 (예: `bu3-trip-schedule`).
4. **Public**으로 설정합니다. (GitHub Pages를 무료로 쓰려면 Public 저장소여야 합니다. Private로 하려면 유료 플랜이 필요할 수 있습니다.)
5. `Create repository` 클릭.

> 반드시 우리 조직 자체의 새 계정/저장소로 진행하세요. 원 제작자의 GitHub 계정을 그대로 쓰면 안 됩니다.

---

## 2. 파일 업로드 + GitHub Pages 활성화

1. 압축파일을 풀어서 10개 코드 파일이 폴더 최상위에 바로 보이는지 확인합니다 (하위 폴더 없이).
2. 방금 만든 저장소 페이지에서 `Add file` → `Upload files` 클릭.
3. 10개 파일(`index.html` 포함, `GAS_backend_code.txt`와 `SETUP_GUIDE.md`는 제외)을 전부 드래그 앤 드롭.
4. 하단의 `Commit changes` 클릭.
5. 저장소 상단 메뉴에서 `Settings` → 좌측 메뉴 `Pages` 이동.
6. `Build and deployment` → `Source`를 `Deploy from a branch`로, `Branch`를 `main` / `(root)`로 선택 후 `Save`.
7. 1~2분 정도 기다리면 같은 화면에 `https://아이디.github.io/저장소이름/` 형태의 주소가 표시됩니다.
8. 그 주소로 접속해서 4개 탭(📅 간트 차트 / 👤 인원 출장일 / 🏭 설비 진행율 / 📋 이력관리)이 빈 상태로 뜨면 성공입니다.

> 파일명 대소문자와 확장자를 그대로 유지하세요. `index.html`이 저장소 루트에 있어야 합니다.

---

## 3. Google Sheets + Apps Script 배포

### 3-1. 스프레드시트 생성
1. 구글 계정으로 로그인한 상태에서 [sheets.new](https://sheets.new)로 새 스프레드시트를 만듭니다.
2. 이름을 정해줍니다 (예: "BU3 출장 일정 DB").

### 3-2. Apps Script 코드 붙여넣기
1. 스프레드시트 메뉴에서 `확장 프로그램` → `Apps Script` 클릭.
2. 열린 편집기의 기존 코드를 전부 지웁니다.
3. 압축파일 안의 `GAS_backend_code.txt` 내용을 전체 복사해서 붙여넣습니다.
4. `Ctrl+S`로 저장합니다.

### 3-3. 웹 앱으로 배포
1. 우측 상단 `배포` → `새 배포` 클릭.
2. 유형 선택에서 톱니바퀴 아이콘 → `웹 앱` 선택.
3. 설명: 원하는 이름 입력 (예: "BU3 데이터 백엔드").
4. **실행 계정: 나** 선택.
5. **액세스 권한: 전체 허용(익명 사용자 포함)** 선택. (팀원들이 별도 로그인 없이 웹앱을 통해 데이터를 주고받아야 하기 때문입니다. 이 URL을 외부에 공개하지 않는 한 URL을 아는 사람만 접근할 수 있습니다.)
6. `배포` 클릭.
7. **처음 배포할 때 "Google에서 확인하지 않은 앱" 경고가 뜰 수 있습니다.** 이건 본인이 직접 만든 스크립트라 정상적인 경고이니, 다음 순서로 진행하세요:
   - 권한 요청 화면에서 본인 구글 계정 선택
   - "Google에서 이 앱을 확인하지 않았습니다" 경고 화면에서 좌측 하단 `고급` 클릭
   - `(프로젝트 이름)(으)로 이동(안전하지 않음)` 클릭
   - 권한 목록 확인 후 `허용` 클릭
8. 배포가 완료되면 `https://script.google.com/macros/s/………/exec` 형태의 **웹앱 URL**이 표시됩니다. 이 URL을 복사해서 잘 보관하세요 (다음 단계에서 사용).

> **코드를 나중에 수정하게 되면**: `배포` → `배포 관리` → 기존 배포 옆 연필(수정) 아이콘 → 버전에서 `새 버전` 선택 → 배포. **"새 배포"를 새로 만들면 URL이 바뀌어서 모든 팀원이 다시 설정해야 하니, 반드시 "새 버전"으로 갱신하세요.**

---

## 4. 앱에서 Google Sheets 연결하기

1. 2단계에서 만든 GitHub Pages 주소로 접속합니다.
2. 상단 툴바의 `⚙ Sheets 설정` 버튼 클릭.
3. `⚙ 설정 / 데이터 관리` 창이 뜨고, "Sheets 연동" 아래 "Apps Script 웹앱 URL을 입력하세요." 안내와 `URL` 입력창이 보입니다.
4. 3-3단계에서 복사해둔 웹앱 URL을 붙여넣습니다.
5. `URL 저장` 버튼 클릭 — 저장과 동시에 자동으로 연결 테스트가 진행됩니다.
6. 화면 상단의 연결 상태 표시(●)가 초록색으로 바뀌면 연결 성공입니다. 클릭하면 언제든 다시 확인할 수 있습니다.

> 이 배포판은 `기본값` 버튼을 누르면 URL이 빈 값으로 초기화됩니다(원본과 달리 미리 채워진 기본 URL이 없습니다). 실수로 누르지 않도록 주의하세요.

---

## 5. 사이트/그룹/프로젝트 초기 등록

상단 툴바의 `사이트 관리` 버튼을 눌러 모달을 엽니다.

**등록 순서를 반드시 지켜주세요:**
1. 좌측의 "새 그룹 추가" 입력창에 그룹명(예: 우리 조직의 고객사명/사업부명)을 입력하고 추가 버튼 클릭. **그룹을 최소 1개 이상 먼저 만들어야** 사이트를 추가할 때 소속 그룹을 선택할 수 있습니다.
2. "새 사이트 추가"에서 이름/색상을 정하고 소속 그룹을 선택한 뒤 추가.
3. 각 사이트 행에서 국가/지역을 선택하면 인원 출장일 탭의 지역별 집계에 반영됩니다.
4. 사이트 우측의 "+ 프로젝트"로 프로젝트를 추가합니다.

> **주의**: 그룹은 "추가"만 화면에 버튼이 있고, 한번 만든 그룹의 **이름 수정이나 삭제 버튼은 현재 화면에 없습니다.** 그룹명은 처음 만들 때 신중하게 정하세요. 오타를 냈다면, 지우는 대신 새 그룹을 다시 만들고 그 그룹에 속한 사이트들을 각각 새 그룹으로 옮기는 방식(사이트 행의 그룹 선택란 변경)으로 우회하세요. 반면 사이트 이름과 프로젝트는 수정·삭제 버튼이 있어 자유롭게 고칠 수 있습니다.

---

## 6. 트러블슈팅

**압축 푼 폴더의 index.html을 더블클릭해서 열었더니 Sheets 연동이 안 됩니다**
→ `file://`로 직접 여는 방식은 지원하지 않습니다. 반드시 GitHub Pages 주소(https)로 접속하세요.

**"Google에서 확인하지 않은 앱"이라는 경고가 계속 뜹니다**
→ 3-3단계 7번의 "고급 → 이동(안전하지 않음)" 절차를 다시 따라주세요. 본인이 만든 스크립트라 정상적인 절차입니다.

**Apps Script 코드를 수정했는데 반영이 안 됩니다**
→ "새 배포"가 아니라 "배포 관리 → 편집(연필) → 새 버전"으로 갱신했는지 확인하세요. 새 배포를 만들면 URL이 바뀌어 버립니다.

**연결 상태 점이 계속 빨간불입니다**
→ 입력한 URL 끝이 `/exec`로 끝나는지, 배포 시 액세스 권한을 "전체 허용"으로 설정했는지 다시 확인하세요.

**그룹 이름을 잘못 만들었는데 못 고칩니다**
→ 5단계의 안내대로, 새 그룹을 만들고 사이트를 그 그룹으로 옮기는 방식으로 우회하세요.

**여러 명이 동시에 쓰면 데이터가 꼬이지 않나요?**
→ 각자의 브라우저가 변경사항을 저장할 때마다 서버(Apps Script)가 기존 데이터와 새로 들어온 데이터를 레코드 단위로 병합합니다. 저장 버튼을 누른 시점을 기준으로 최신 데이터가 반영되므로, 일반적인 동시 편집 상황에서는 데이터가 갑자기 사라지거나 되돌아가지 않도록 설계되어 있습니다.

**실수로 원 제작자(다른 조직)의 웹앱 URL을 그대로 쓰면 안 되나요?**
→ 절대 안 됩니다. 그 경우 우리가 등록한 데이터가 원 제작자의 구글시트에 저장되어 정보가 섞이거나 유출됩니다. 반드시 3단계에서 우리 조직이 직접 발급받은 URL만 사용하세요.
