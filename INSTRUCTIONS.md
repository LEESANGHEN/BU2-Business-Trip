# BU2 출장일정관리 — 배포 가이드

## 무엇이 바뀌었나

- 기존 BU2 Portal의 "출장일정관리" 링크는 BU3 전용 앱(`pwc-ship-it/trip`)을 그대로 가져다 쓰고
  있었고, 그 앱은 BU3의 실제 사이트/프로젝트/출장 인원 데이터(그리고 BU3 내부 서버 바로가기)로
  채워져 있었습니다. BU2와 BU3가 같은 데이터를 공유하는 구조였습니다.
- 이번에 만든 `BU2-trip` 폴더는 **BU3와 완전히 분리된 새 인스턴스**입니다.
  - 사이트/프로젝트/출장일정/이벤트: 전부 빈 상태로 시작 (BU2 데이터를 새로 입력)
  - 상단 "🔗 바로가기" 메뉴: BU3 내부 서버 IP 대신, BU2 Portal에 있는 실제 링크들로 교체
  - 문구: "BU3 출장 일정 관리" → "BU2 출장 일정 관리" 등 표기 변경
  - 데이터 저장 백엔드: BU3가 쓰는 Google Sheet가 아니라, **새로 만드는 BU2 전용 Google Sheet**

아래 순서대로 진행하면 됩니다. 1~2단계는 Google/GitHub 계정 권한이 필요해서 직접 해주셔야 하고,
코드/문구는 이미 다 준비되어 있습니다.

---

## 1단계. BU2 전용 데이터 저장소(Google Sheet) 만들기

1. https://sheet.new 로 새 Google Sheet 생성 (이름 예: `BU2 출장일정관리 DATA`)
2. 상단 메뉴 **확장 프로그램 → Apps Script**
3. 기본으로 열려 있는 `Code.gs`의 내용을 전부 지우고, 함께 받은 **`Code.gs` 파일 내용을 그대로
   붙여넣기**
4. 저장(💾) 후 오른쪽 위 **배포 → 새 배포**
   - 유형: **웹 앱**
   - 실행할 사용자: **나**
   - 액세스 권한이 있는 사용자: **전체** ← 꼭 "전체"로 선택 (로그인 없이 앱이 접근해야 함)
5. **배포** 클릭 → 권한 승인 화면에서 본인 계정으로 승인
6. 배포 완료 후 나오는 **웹 앱 URL**을 복사
   (`https://script.google.com/macros/s/여기부분/exec` 형태)

## 2단계. 앱 코드에 URL 반영하기

`script.js` 파일에서 아래 줄을 찾아서:

```js
var DEFAULT_SHEETS_URL='';
```

1단계에서 복사한 URL로 바꿔주세요:

```js
var DEFAULT_SHEETS_URL='https://script.google.com/macros/s/여기부분/exec';
```

(이 값을 비워두면 각자 브라우저에만 저장되고 팀원 간 공유가 안 됩니다. 반드시 채워주세요.)

## 3단계. GitHub Pages로 배포하기

1. GitHub에서 본인 계정으로 새 저장소 생성 (예: `bu2-trip`)
2. 이 폴더의 파일들(`index.html`, `app.js`, `data.js`, `equip.js`, `export.js`, `gantt.js`,
   `modals.js`, `person.js`, `script.js`, `style.css`, `README.md`)을 저장소 루트에 업로드
   — `Code.gs`와 `INSTRUCTIONS.md`는 Google Sheet 쪽에서만 쓰는 파일이라 GitHub Pages에는
   올리지 않아도 됩니다.
3. 저장소 **Settings → Pages**에서 Source를 `main` 브랜치 / `/ (root)`로 설정 후 저장
4. 몇 분 후 `https://<계정명>.github.io/bu2-trip/` 형태의 주소로 접속 가능해집니다

## 4단계. BU2 Portal의 링크 수정하기

1. BU2 Portal(https://bu-2-portal-git-main-2-23ca.vercel.app/) 접속
2. 우측 상단에서 관리자 모드로 전환 (관리자 모드 진입 방법은 기존에 쓰시던 대로)
3. "출장일정관리" 항목을 편집(✏️) → URL을 3단계에서 만든 GitHub Pages 주소로 교체 → 저장

## 5단계. 확인

1. 새 주소로 접속해서 정상적으로 열리는지 확인 (사이트/프로젝트/일정이 비어있는 게 정상입니다)
2. 상단 "사이트 관리" / "+ 출장 등록" 등으로 BU2 실제 데이터를 입력
3. 페이지를 새로고침해서 입력한 데이터가 유지되는지 확인 (Google Sheet에도 데이터가
   쌓이는지 Apps Script를 붙여넣은 시트를 열어서 확인 가능 — `BU2_TRIP_DATA`라는 이름의
   숨겨진 시트 탭에 저장됩니다)

---

## 참고: 왜 BU3 원본 앱을 그대로 고치지 않았나요?

- `pwc-ship-it/trip` 저장소는 BU3 쪽 계정 소유라서 이 작업에서는 코드를 직접 수정할 권한이
  없었습니다.
- 설령 권한이 있어도, 원본 앱은 BU3와 **같은 Google Sheet**를 데이터 저장소로 쓰고 있어서
  그 안의 데이터만 바꾸면 BU3가 쓰던 실제 출장 데이터가 사라지는 문제가 있었습니다.
- 그래서 코드/화면은 그대로 재사용하면서, 데이터 저장소만 BU2 전용으로 새로 분리하는
  방식을 택했습니다.

## 참고: 데이터 동기화 방식의 제한사항

새로 만든 `Code.gs` 백엔드는 저장할 때마다 전체 데이터를 통째로 저장/불러오는 단순한
방식입니다. BU3 원본이 쓰는 정교한 "레코드 단위 병합 + 삭제 기록" 서버 로직은 Google 쪽에
비공개로 배포되어 있어 소스를 볼 수 없었기 때문에 그대로 옮기지 못했습니다. BU2처럼 소수
인원이 순차적으로 입력하는 규모라면 문제없이 동작하지만, 여러 사람이 정확히 동시에 각자
다른 PC에서 저장을 누르는 극히 드문 경우에는 마지막 저장이 우선합니다.
